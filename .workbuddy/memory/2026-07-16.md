# 2026-07-16 工作日志

## 10:33 Supabase 免费项目保活（GitHub Actions）
- 需求：Supabase 免费项目 7 天无活动会自动暂停，用 GitHub Actions 每 3-4 天 ping 一次保活。
- 实现：新增 `.github/workflows/supabase-keep-alive.yml`
  - cron `0 9 * * 1,4`（周一、周四 09:00 UTC = 北京 17:00），间隔 3 天，低于 7 天阈值；带 `workflow_dispatch` 可手动触发。
  - 用 Supabase REST API 依次 `SELECT id FROM quotations/products/customers LIMIT 1`，任一返回 200 即成功。
  - `SUPABASE_URL` 硬编码（`https://miwkbsziywwguehqgwma.supabase.co`）；仅需 1 个 repo Secret：`SUPABASE_ANON_KEY`。
- 踩坑：旧 token（`ghp_fCzm...`）无 `workflow` scope，push `.github/workflows/` 被 GitHub 拒绝。用户新建带 `repo`+`workflow` 的新 token（`ghp_6uJO...`）后 push 成功。
- 验证：手动 dispatch → run 29466905603 → 4 个 step 全 success，HTTP 200。已跑通。
- 提交：`bf1e557`。用户已在 repo 配好 `SUPABASE_ANON_KEY` Secret。

## 15:57 添加浏览器标签页 favicon
- 需求：浏览器标签页没有 ico 图标，截图显示只有文字“云伟”。
- 实现：
  - 用 `scripts/generate_favicon.py`（Pillow）生成蓝色背景 + 白色“YW”字母的 favicon.ico，包含 16/32/48/64 多尺寸。
  - 放置于 `app/favicon.ico`，Next.js 15 自动识别为页面图标，无需额外 metadata 配置。
  - 本地 `npm run build` 通过。
- 提交：`bcc3127`；已用新 token（`ghp_6uJO...`）推送到 main，Vercel 自动部署中。
- 部署完成后刷新页面，浏览器标签页应显示蓝色 YW 图标。

