# 2026-07-20 工作日志

## 11:13 修复 PI 页面下载按钮无响应
- 现象：形式发票 (PI) 列表页点击「下载」按钮没有反应；报价单页面下载正常。
- 根因：
  - PI 页面原本使用硬编码 `MOCK_PI` 数据，下载按钮没有绑定 `onClick`。
  - 报价单页面早已从 Supabase 加载真实数据并调用 `/api/quotations/{quote_no}/pdf?...` 下载 PDF。
- 修复：
  - 重写 `app/proforma-invoices/page.tsx`：用 `data.quotations.listPIs()` 从数据库加载 `pi_no IS NOT NULL` 的报价单，渲染客户、国家、金额、状态、日期。
  - 给下载按钮绑定 `downloadPi(q)`：打开 `/api/quotations/{quote_no}/pdf?lang={lang}&kind=pi`。
  - 新增 `lib/supabase/data.ts → quotations.listPIs()` 查询带 `customer:customers(*)` 和 `items:quotation_items(*)` 的 PI 列表。
  - `types/index.ts` 给 `Quotation` 增加可选 `customer?: Customer`，让嵌套 customer join 有类型。
  - 增加加载中、空列表提示、加载失败 toast。
- 本地 `npm run build` 通过。
- 提交：`b860cf5`；已用新 token 推送到 main，Vercel 自动部署中。
- 待验证：部署后到生产环境打开「形式发票 PI」页，点击下载应正常弹出 PI PDF。
